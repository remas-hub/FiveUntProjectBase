<?xml version="1.0" encoding="utf-8"?>
<!-- صف نتيجة حرف داخل تفاصيل جلسة الاختبار -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:baselineAligned="false"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:layout_marginBottom="8dp"
    android:background="@drawable/bg_outcome_row">

    <TextView
        android:id="@+id/tvOutcomeEmoji"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="28sp"
        android:paddingEnd="12dp"
        android:includeFontPadding="false" />

    <FrameLayout
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:background="@drawable/bg_letter_circle_soft">

        <TextView
            android:id="@+id/tvOutcomeLetter"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:textAlignment="center"
            android:textDirection="ltr"
            android:textColor="#1B5E20"
            android:textSize="24sp"
            android:textStyle="bold"
            android:includeFontPadding="false" />
    </FrameLayout>

    <TextView
        android:id="@+id/tvOutcomeLabel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:paddingStart="14dp"
        android:textColor="#37474F"
        android:textSize="18sp"
        android:lineSpacingExtra="4dp" />
</LinearLayout>
